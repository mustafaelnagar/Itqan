{
  "name": "@itqan/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types for Itqān — the single source of truth across mobile, backend, AI, and admin.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"@itqan/types: type-only package, no runtime tests\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@itqan/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3"
  }
}
